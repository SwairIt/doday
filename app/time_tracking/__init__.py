"""Time-tracking timer per task: start/stop with persisted entries.

A user can run a timer on at most one task at a time (UI hint, not a hard
constraint at the model layer — multiple open entries just sum naturally).
"""
