"""Doday Blog — SEO-раздел /blog.

Статьи лежат как markdown-файлы в ``app/blog/content/<категория>/<slug>.md``
с простым frontmatter. При первом обращении они парсятся в HTML
(markdown-it-py), получают оглавление, время чтения и кэшируются в памяти.
БД не используется — контент едет вместе с кодом через git.
"""
